# Telegram-Claude-Bridge Configuration

telegram:
  # Bot token from @BotFather
  bot_token: "8730903546:AAFl_vWSN_OUUTLuUONzvV0zKyZaecJk54E"

  # Max message length for Telegram (will auto-split longer messages)
  max_message_length: 4096

# Authorized users (Telegram user IDs)
whitelist:
  - 1301659279
  # - 987654321

# Claude API settings
claude:
  # Anthropic API key (or set ANTHROPIC_API_KEY env var)
  api_key: ""

  # Model to use
  model: "claude-sonnet-4-20250514"

  # Max tokens per response
  max_tokens: 16384

  # Timeout in seconds
  timeout: 300

  # System prompt
  system_prompt: |
    Du bist ein hilfreicher Assistent für byTelCom.
    Du hast Zugriff auf MCP-Tools für CRM, Rechnungen, Lohnabrechnung und mehr.
    Antworte auf Deutsch, es sei denn der Nutzer schreibt auf Englisch.

  # MCP server (stdio)
  mcp_server:
    command: "py"
    args: ["-3.14", "C:/Users/Micha/claude/byMCP/server.py"]

# Session settings
session:
  # Minutes of inactivity before starting a new conversation
  timeout_minutes: 60
